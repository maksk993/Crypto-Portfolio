<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CryptoPortfolioNight" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/background_color</item>
        <item name="android:navigationBarColor">@color/black</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="bottomNavigation">
        <item name="android:background">@color/first_color</item>
        <item name="itemTextColor">@color/white</item>
        <item name="itemIconTint">@color/white</item>
    </style>
    <style name="background">
        <item name="android:background">@color/background_color</item>
    </style>
    <style name="firstColor">
        <item name="android:background">@color/first_color</item>
    </style>
    <style name="secondColor">
        <item name="android:background">@color/second_color</item>
    </style>
    <style name="firstTextColor">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="secondTextColor">
        <item name="android:textColor">@color/second_color</item>
    </style>
</resources>